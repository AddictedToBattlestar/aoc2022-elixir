defmodule Day7.Part1 do
  # def getSumSizeOfDirectory(sizeThreshold) do
  #   inputDataLines = getLineData()
  #   directoryMap = buildDirectoryMap(inputDataLines)
  # end

  # defp getLineData() do
  #   {:ok, contents} = File.read("input.data")
  #   contents |> String.split("\n")
  # end

  # defp buildDirectoryMap(inputDataLines) do
  #   Enum.reduce(lineData, %{currentDirectory: "/", directoryMap: %{}}, fn(line, accumulator) ->

  #   end)
  # end
end
